import { ZoomInIconTemplate } from './ZoomIn.template'
import { ZoomInIconProps } from './ZoomIn.types'

export const ZoomInIcon = ({ ...props }: ZoomInIconProps) => {
  return <ZoomInIconTemplate {...props} />
}
